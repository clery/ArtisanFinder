[x] Add a multi-character system, to allow crafters to show if they are available but on a alt character
[] Think of a way to advertise a multi-character "shop", so people may find you, and potentially a more complete description of your specializations and such.
[] Add guild functionnality so that if a guild member is connected and has the right profession, they show up in the customer view in a way that indicates they are guild members.

[x] Check if changing items rescans all recipes. -> It doesn't. Should it ?

[x] There is a strange overlap between "Offline (last seen ...)" and "found 3h ago". Remove the (last seen...) part, since it is redundant with the "found 3h ago"
[x] ChatThrottleLib should never be missing since it is a dependency. Remove the safeguard
[] Check for a way to ensure an artisan is offline instead of relying on time since seen.