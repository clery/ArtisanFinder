[] Add reagent icon next to reagent name in tooltip
[] Wait until knowledge is applied before triggering a new scan
[] Increase font and icon size in customer view and tooltip
[] Widen the customer view to allow the sort button to be larger, as it is currently too small for "Sort: Recommended"
[] Avoid scan running on every profession opening
[] Make scan as a background process to avoid freezing the game

[] Add a multi-character system, to allow crafters to show if they are cached but on a alt character
[] When opening a profession link from someone else, the addon scans it, but binds it to my character. Either prevent scanning or bind it to the character whose link I clicked.